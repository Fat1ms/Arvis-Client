# Arvis AI Assistant - Python Dependencies

# ==============================================
# CORE GUI FRAMEWORK
# ==============================================
# Migrated from PyQt5 to PyQt6 (21.10.2025)
PyQt6==6.7.1
PyQt6-sip==13.8.0
# Note: PyQt6-Qt6 is optional, auto-selected by PyQt6
# PyQt6-Qt6
# PyQt6-SVG  # Optional, may not be available for all Python versions

# ==============================================
# AI AND MACHINE LEARNING
# ==============================================
# PyTorch (install separately with specific version for your system)
# For CPU: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# For GPU: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchaudio>=2.0.0

# ==============================================
# SPEECH SYNTHESIS (TTS)
# ==============================================
# Silero TTS (Russian, fast, offline)
# pip install silero models will be downloaded by torch.hub
# No explicit package needed - use torch.hub.load()

# Bark TTS (Multilingual, offline, slower)
# Note: Very large model (~7GB), lazy loading recommended
bark-ml>=0.1.0  # Bark text-to-speech

# SAPI5 Fallback (Windows TTS)
# Primary option - pure Python, no dependencies
pyttsx3>=2.90  # Text-to-speech library (Windows/Mac/Linux support)

# Windows-only: direct SAPI COM access (optional, for advanced use)
# pywin32>=306  # Automatically installed as dependency

# ==============================================
# SPEECH RECOGNITION (VOSK)
# ==============================================
# Note: vosk may not be available for Python 3.13+
# For offline Russian speech recognition
vosk>=0.3.45; python_version < "3.13"  # Offline speech recognition (Russian language)
srt>=3.5.3  # For subtitle processing if needed

# ==============================================
# AUDIO PROCESSING
# ==============================================
soundfile==0.12.1
sounddevice==0.4.6
numpy<2  # For audio data processing; PyTorch wheels on Windows require NumPy 1.x
# PyAudio - НЕСОВМЕСТИМ С PYTHON 3.13!
# Для Python 3.11-3.12: pip install pyaudio==0.2.13
# Для Python 3.13+: pip install pipwin; pipwin install pyaudio
# Или используйте предсобранный wheel: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# pyaudio==0.2.13  # Закомментировано для Python 3.13 compatibility
omegaconf>=2.3.0  # Required by Silero TTS hub scripts (subprocess worker)

# ==============================================
# HTTP REQUESTS AND APIs
# ==============================================
requests==2.31.0
urllib3>=2.0.0
certifi>=2023.0.0

# ==============================================
# SECURITY AND AUTHENTICATION
# ==============================================
pyotp>=2.9.0  # TOTP for 2FA
qrcode>=7.4.2  # QR code generation for 2FA
Pillow>=10.0.0  # Image support for QR codes

# ==============================================
# SYSTEM INFORMATION AND CONTROL
# ==============================================
psutil==5.9.6
# For Windows system control
pywin32>=306; sys_platform == "win32"

# ==============================================
# DATE AND TIME
# ==============================================
python-dateutil==2.8.2

# ==============================================
# ENVIRONMENT VARIABLES
# ==============================================
python-dotenv==1.0.0

# ==============================================
# DATA PROCESSING
# ==============================================
# JSON уже встроен в Python
# pathlib уже встроен в Python

# ==============================================
# DEVELOPMENT AND TESTING (OPTIONAL)
# ==============================================
pytest==7.4.4
pytest-qt==4.3.1  # Compatible with both PyQt5 and PyQt6
pytest-asyncio==0.21.1

# ==============================================
# SECURITY AND AUTHENTICATION
# ==============================================
# Для 2FA (TOTP)
pyotp>=2.9.0
# Для JWT токенов
pyjwt>=2.8.0
# Для QR кодов (setup 2FA)
qrcode>=7.4.2
pillow>=10.0.0  # Required by qrcode for image generation

# ==============================================
# ADDITIONAL UTILITIES
# ==============================================
# Для работы с архивами (например, распаковка моделей)
zipfile36==0.1.3

# Для криптографии (если понадобится)
cryptography>=41.0.0

# Installation Notes:
# 1. Install PyTorch with CUDA support if you have NVIDIA GPU:
#    pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 2. For CPU-only PyTorch installation:
#    pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 3. Download Vosk model manually:
#    wget https://alphacephei.com/vosk/models/vosk-model-ru-0.42.zip
#    unzip to models/vosk-model-ru-0.42/
#
# 4. For system audio control, you might need additional tools:
#    Download NirCmd from https://www.nirsoft.net/utils/nircmd.html
#    Place nircmd.exe in PATH or in application directory
#
# 5. API Keys needed:
#    - OpenWeatherMap API: https://openweathermap.org/api
#    - NewsAPI: https://newsapi.org/
